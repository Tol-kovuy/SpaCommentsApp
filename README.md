# SpaApp - Comments Management System

## Описание

SpaApp - это многослойное монолитное SPA-приложение для управления комментариями, построенное на основе Domain Driven Design (DDD) и ABP Framework. Приложение предоставляет полнофункциональную систему комментариев с поддержкой древовидной структуры, валидацией, CAPTCHA и загрузкой файлов.

## Технический стек

### Backend
* **.NET 9.0+** - основная платформа
* **ABP Framework** - enterprise-фреймворк
* **Entity Framework Core** - ORM
* **MS SQL Server** - база данных
* **ASP.NET Core** - веб-API

### Frontend
* **Angular** - фреймворк для SPA
* **TypeScript** - язык программирования
* **RxJS** - реактивное программирование

## Функциональность

### 📝 Система комментариев
* Добавление комментариев с полной валидацией данных
* Древовидная структура с поддержкой ответов и ветвлений
* Пагинация и сортировка комментариев
* Система очередей (CommentQueue) для обработки

### 🔐 Безопасность и валидация
* CAPTCHA защита от автоматических отправок
* Валидация полей на стороне клиента и сервера
* Поддержка HTML тегов с белым списком (`<a>`, `<code>`, `<i>`, `<strong>`)
* Защита от XSS и SQL-инъекций

### 📎 Работа с файлами
* Загрузка изображений (JPG, PNG, GIF) с автоматическим ресайзом до 320×240px
* Текстовые файлы (TXT) до 100KB
* Предпросмотр прикрепленных файлов

### 👤 Управление пользователями
* Обязательные поля: Имя пользователя (латинские буквы и цифры), Email
* Опциональные поля: Домашняя страница (URL)
* Валидация форматов в реальном времени

## Предварительные требования

* [.NET 9.0+ SDK](https://dotnet.microsoft.com/download/dotnet)
* [Node.js v18 или v20](https://nodejs.org/en)
* MS SQL Server

## Настройка и запуск

### 1. Настройка подключения к БД
Проверьте и при необходимости измените строки подключения в файлах `appsettings.json` в проектах:
* `SpaApp.HttpApi.Host`
* `SpaApp.DbMigrator`

### 2. Установка зависимостей
```bash
# Установка клиентских зависимостей
abp install-libs
3. Миграция базы данных
bash
# Запуск мигратора для создания начальной БД
dotnet run --project src/SpaApp.DbMigrator
4. Генерация сертификата подписи (для продакшена)
bash
dotnet dev-certs https -v -ep openiddict.pfx -p 96dac333-36eb-472d-80ae-4f8d01ed78b3
5. Запуск приложения
Backend:

bash
dotnet run --project src/SpaApp.HttpApi.Host
Frontend:

bash
cd angular
npm start
API Endpoints
Комментарии
GET /api/app/comment - получение списка с пагинацией и сортировкой

POST /api/app/comment - создание нового комментария

GET /api/app/comment/{id}/replies - получение ответов

Файлы
POST /api/app/file/upload - загрузка файлов

GET /api/app/file/{id} - скачивание файла

CAPTCHA
GET /api/app/captcha/generate - генерация CAPTCHA

POST /api/app/captcha/validate - валидация ввода

Деплой
Процесс деплоя соответствует стандартному процессу деплоя .NET и Angular приложений. Для детальной информации обратитесь к документации по деплою ABP.

Дополнительные ресурсы
Внутренние ресурсы
Документация по Angular интеграции

Внешние ресурсы
Tutorial по веб-разработке

Application Startup Template

Особенности реализации
DDD архитектура с четким разделением ответственности

Модульная структура для легкого расширения функциональности

Реактивные формы Angular с комплексной валидацией

Поддержка очередей для асинхронной обработки комментариев

Полноценная система безопасности на основе ABP Framework


    ┌─────────────────┐       ┌──────────────────┐
    │  AppComments    │       │  CommentFiles    │
    ├─────────────────┤       ├──────────────────┤
    │ • Id (PK) ◄─┐   │       │ • Id (PK)        │
    │ • ParentId  │   │       │ • FileName       │
    │ • UserName  │   │1    *│ • FileType       │
    │ • Email     │   └──────┼ • CommentId (FK) │
    │ • Homepage  │          │ • FileSize       │
    │ • Text      │          │ • Width          │
    │ • Creation  │          │ • Height         │
    └─────────────────┘       └──────────────────┘
          │
          └◄─ (рекурсивная связь)
